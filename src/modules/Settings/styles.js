import { proportionHeight as ph } from '_helpers/dimensions';

import { GREY_TEXT } from '_constants/colors';

const settingsImg = {
  marginTop: ph(70)
};

const buttonsTextStyle = {
  color: GREY_TEXT,
  fontSize: 15,
  textAlign: 'center'
};

export { settingsImg, buttonsTextStyle };
